{
  "name": "salud-clinical-solutions",
  "version": "1.0.0",
  "description": "",
  "browserslist": {
    "production": [
      ">0.5%",
      "last 2 versions",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "clean": "rm -rf .parcel-cache dist",
    "dev": "parcel index.html --open",
    "build": "parcel build index.html --no-cache --public-url ./",
    "serve": "parcel serve index.html --no-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.16.1",
    "posthtml": "^0.16.7",
    "posthtml-include": "^2.0.1"
  }
}
